---
id: useDebounce
title: useDebounce
slug: '/'
---

用来处理防抖函数的hook

## 代码演示
### 基础用法

import FrameWrap from '../components/frame-wrap'
import IuseDebounce from '../components/use-debounce/useDebounce'
const useDebounceCode = require('!!raw-loader!../components/use-debounce/useDebounce')
const dsc = '频繁调用 run，但只会在所有点击完成 1000ms 后执行一次相关函数, 在函数执行前，点击取消btn后则取消执行'

<FrameWrap dsc={dsc}
  example={<IuseDebounce />}
  code={useDebounceCode.default} />


### Params

import { Table } from 'antd'
import { dataParams, paramsColumns } from '../components/use-debounce/config'

<Table dataSource={dataParams} columns={paramsColumns} pagination={false} />

### Result

import { dataResult, resultColumns } from '../components/use-debounce/config'

<Table dataSource={dataResult} columns={resultColumns} pagination={false} />

